# Python package

`Pool` reads one worker stream. `KernelEnv` adds synchronous action boundaries.
`Batch` owns its tensor storage. See the [batch contract](../../docs/batches.md),
[action windows](../../docs/action-windows.md), and [IDE setup](../../docs/ide.md).
