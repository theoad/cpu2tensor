# Python interface and examples

This directory contains the `cpu2tensor` package, Python tests, and client examples.
See [the quickstart](../docs/quickstart.md), [validation](../docs/validation.md),
and [the current slice](../docs/backlog.md).

This directory is the single Python source root. Packaging metadata lives at
the repository root. See [IDE and interpreter setup](../docs/ide.md).
