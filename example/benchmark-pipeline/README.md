# Tensor pipeline benchmark

Measure operator-started worker endpoints through CPU, MPS or CUDA tensor
consumption. See the [pipeline guide](../../docs/benchmark-pipeline.md).
The installed module is `cpu2tensor.examples.benchmark_pipeline`.
