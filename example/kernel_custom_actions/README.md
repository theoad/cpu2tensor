# Custom kernel actions

Run two bounded `openat` sequence variants through one synchronous kernel episode.
The client owns the mutation grammar and result checks.

See the [guest contract, setup, and limits](../../docs/custom-kernel-actions.md).
