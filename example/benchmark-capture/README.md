# Capture overhead

Compare vanilla QEMU with selected signals on the same host. The benchmark drains
traces without building tensors and saves repeated timings, resource usage and
completion checks.

See [the manifest, command and measurement limits](../../docs/benchmark-capture.md).
