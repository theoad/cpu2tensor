# Blocks with paging context

See [the context-only guide](../../docs/context-only.md) for worker setup,
per-source tensor association and measured costs. Run the installed client with
`python -m cpu2tensor.examples.observe_context --endpoint ENDPOINT`.
