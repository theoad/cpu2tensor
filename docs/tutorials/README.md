# Notebook tutorials

- [Executable layout and learned positions](normalization.ipynb): add a native
  launch event, receive an owned tensor and measure a small learning experiment.

Use the repository's installed Python package and a Jupyter kernel from that
environment. The notebook records its dependencies, evidence and limits.
